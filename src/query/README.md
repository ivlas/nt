# query

Parses and evaluates deterministic, case-insensitive, AND-combined query
expressions against SQLite-loaded note metadata and bodies. There is no ranking,
fuzzy matching, semantic search, or hidden retrieval.
